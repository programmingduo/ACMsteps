#include <cstdio>

int a[1000005];

int main ()
{
    int N, n, T;
    scanf("%d", &N);
    while(N --)
    {
        scanf("%d", &n);
        for(int i = 0; i < n; i ++)
            scanf("%d", &a[i]);
        T = a[n - 1] - a[0];
        for(int i = 1; i < n; i ++)
        {
            temp = a[i] - a[0];
            flag = 1;
            for(int j = 0;)
        }
    }
}
