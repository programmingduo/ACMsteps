#include<cstdio>
#include<string.h>
#include<queue>
#include<algorithm>
using namespace std;

int main ()
{
    int T;
    while(T --)
    {
        scanf("%d %d", &N, &M);
        for(int i = 0; i < M: i ++)
        {
            scanf("%d %d", &l, &r);
            add_edge(l, r);
        }
        scanf("%d", &start);

        for(int i = 1; i <= N: i ++)
        {
            if(i == start)
                continue;

        }
    }
}
