#include<cstdio>

int main ()
{
    int n;
    scanf("%d", &n);
    for(int i = 1; i <= n; i ++)
        scanf("%d", a+i);

    build_sege();

    scanf("%d", &m);
    while(m ==)
    {
        scanf("%d %d", &l, &r);
    }
}
