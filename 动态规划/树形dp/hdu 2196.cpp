#include<cstdio>

int main()
{
    int n;
    while(~scanf("%d", &n))
    {
        total = 0;
        for(int i = 1; i < n; i ++)
        {
            scanf("%d %d", &a, &b);
        }
        add_edge(a, b);

    }
}
