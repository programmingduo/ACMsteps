#include<cstdio>

int main ()
{
    int T;
    scanf("%d", &T);
    while(T --)
    {
        scanf("%d %d %d", &n, &a, &b);
        for(int i = 1; i <= n; i ++)
        {
            scanf("%d", &a[i]);
        }


    }
}
