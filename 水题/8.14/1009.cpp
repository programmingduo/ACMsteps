#include<cstdio>

int main()
{
    int n, m, T;
    scanf("%d", &T);
    for(int t = 1; t <= T; t ++)
    {
        scanf("%d %d", &n, &m);
        for(int i = 1; i <= n; i ++)
        {
            scanf("%d", &a[i]);
        }
        while(m --)
        {
            scanf("%d %d",)
        }
    }

}
