#include<cstdio>

int main ()
{
    int k, n, m;
    while(~scanf("%d", &k) && k)
    {
        scanf("%d %d", &n, &m);
        for(int i = 0; i < k; i ++)
        {
            scanf
        }
    }
}
